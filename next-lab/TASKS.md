# Задания по Next.js

Двадцать упражнений. Правило одно: **сначала сломай, потом почини**.
Понимание приходит от увиденной ошибки, а не от прочитанного абзаца.

После каждого задания вслух: что я изменил и почему это сработало.
Отмечай сделанное галочкой прямо в этом файле.

---

## Блок 1 — граница сервер/клиент

- [ ] **NX-01.** В `app/01-server-component/page.tsx` добавь `const [n, setN] = useState(0)`.
      Запусти. Прочитай текст ошибки целиком и объясни его своими словами. Верни как было.

- [ ] **NX-02.** Туда же добавь `console.log('привет')`. Найди, **где** он напечатался.
      Объясни, почему не в консоли браузера.

- [ ] **NX-03.** В `app/02-client-component/counter.tsx` убери первую строку `'use client'`.
      Получи ошибку сборки, прочитай её. Верни директиву.

- [ ] **NX-04.** Попробуй импортировать серверный компонент внутрь `counter.tsx`.
      Получи ошибку. Затем реши задачу правильно: сделай `<Counter>` принимающим `children`
      и передай туда серверную разметку из `page.tsx`.

- [ ] **NX-05.** Передай в `<Counter />` пропсом функцию `() => 1`. Получи ошибку сериализации.
      Объясни, почему функции через границу не ходят и какое есть исключение.

## Блок 2 — загрузка и ошибки

- [ ] **NX-06.** В `app/03-streaming/page.tsx` перенеси `await getPosts(1500)` из `SlowBlock`
      прямо в тело страницы. Убедись, что стриминг пропал и ждать приходится всю страницу.
      Верни как было и сформулируй правило.

- [ ] **NX-07.** Удали `app/03-streaming/loading.tsx`. Что изменилось при переходе на страницу?
      Верни файл.

- [ ] **NX-08.** Добавь третий медленный блок со своей задержкой и своим `<Suspense>`.
      Проверь, что блоки появляются независимо друг от друга.

- [ ] **NX-09.** В `app/04-error/error.tsx` убери `'use client'`. Получи ошибку — она объясняет,
      почему граница ошибок обязана быть клиентской. Верни директиву.

- [ ] **NX-10.** Сделай так, чтобы `getFlaky` падала через раз (например, по `Math.random()`).
      Проверь, что кнопка «Повторить» иногда помогает, а иногда нет. Объясни, что делает `reset()`.

## Блок 3 — маршруты

- [ ] **NX-11.** В `app/05-posts/[id]/page.tsx` убери `await` у `params`.
      Посмотри, что придёт в `id`. Верни `await` и запомни это навсегда.

- [ ] **NX-12.** Создай маршрут `app/05-posts/[id]/edit/page.tsx`, который показывает
      заголовок поста и надпись «Редактирование». Проверь, что `params` виден и здесь.

- [ ] **NX-13.** Сделай catch-all маршрут `app/docs/[...slug]/page.tsx`, который выводит
      все сегменты пути списком. Проверь на `/docs/a/b/c`.

- [ ] **NX-14.** Убери `generateStaticParams` из `[id]/page.tsx`, пересобери проект и сравни
      таблицу маршрутов: было `●`, стало `ƒ`. Объясни разницу.

- [ ] **NX-15.** Добавь на страницу поста `loading.tsx` и убедись, что при переходе
      из списка видно скелет (в `getPost` уже стоит задержка 200 мс).

## Блок 4 — формы и данные

- [ ] **NX-16.** В `app/08-server-action/page.tsx` убери `revalidatePath`.
      Добавь пост и объясни, почему список не обновился.

- [ ] **NX-17.** Отключи JavaScript в devtools и отправь форму из демо 08.
      Убедись, что она работает. Это и есть прогрессивное улучшение.

- [ ] **NX-18.** В `app/09-action-state/actions.ts` добавь третье поле с валидацией
      (например, автор минимум 2 символа) и выведи его ошибку в форме.

- [ ] **NX-19.** Дёрни `/api/posts` из терминала: GET, потом POST с корректным телом,
      потом POST с пустым `title`. Проверь коды ответов 200, 201, 422.

      ```bash
      curl http://localhost:3000/api/posts
      ```

- [ ] **NX-20.** Объясни на своём коде, почему форма из демо 08 не нуждается в `/api/posts`,
      и в каком случае этот эндпоинт всё-таки нужен.

---

## Финальная проверка

Когда все галочки стоят — закрой все файлы и расскажи вслух, без подглядываний:

1. Что произойдёт, если поставить `'use client'` в корневом `layout.tsx`.
2. Почему `params` — промис и как его читают в клиентском компоненте.
3. Три способа обновить данные после мутации и чем они отличаются.
4. Когда нужен Route Handler, а когда достаточно серверного компонента.
5. Почему `error.tsx` обязан быть клиентским, а `layout.tsx` — нет.

Ответы — в [THEORY.md](./THEORY.md), но сначала попробуй сам.
