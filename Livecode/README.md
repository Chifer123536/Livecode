# Livecode — тренажёр лайвкода

518 задач с автотестами: JS, TypeScript, React, алгоритмы, браузер. Прогресс считается по
результатам тестов, а не по галочкам. Работает из редактора, браузер не нужен.

---

## Старт

```bash
yarn install
yarn solve
```

`yarn solve` — весь рабочий цикл одной командой: открывает следующую нерешённую задачу в VS Code
и запускает тесты **только этой задачи** в watch-режиме. На экране одна функция и её условие,
в терминале три строки вместо пятисот.

Стираешь `todo()`, пишешь тело, сохраняешь — тесты пересчитались. Зелёное → `Ctrl+C` и снова `yarn solve`.

Конкретная задача — `yarn solve ARR-14`.

---

## Как это устроено

Один пак — одна тема, одна задача — один файл:

```
src/drills/02-arrays/
├─ pack.json        код пака, уровень, норматив по времени
├─ tasks/
│  ├─ ARR-01.ts     ← ЗДЕСЬ ПИШЕШЬ ТЫ: одна задача, одна функция
│  ├─ ARR-02.ts
│  └─ …
├─ tasks.ts         бочка с реэкспортами, руками не трогать
├─ tasks.test.ts    тесты, они же формальное условие задачи
└─ solution.ts      эталон + разбор, открывать после своей попытки
```

Звёзды — сложность: ★☆☆ разогрев, ★★☆ типовая задача с собеса, ★★★ на подумать.

Четыре вещи, которые надо знать про механику:

1. **Тесты — это условие.** Не понял формулировку в комментарии — читай тесты, там ответ.
2. **Прогресс не врёт.** `yarn progress` гоняет все тесты и считает сданным только то,
   что реально зелёное. Ручных галочек в системе нет.
3. **Задача сдана, когда зелены тесты И типы.** В паках `TS` и `QZ` проверяет не рантайм,
   а компилятор — про них ниже.
4. **`yarn verify` проверяет сам тренажёр:** гоняет тесты против эталонов и типы решений.
   Если он красный — сломан тренажёр, а не ты.

---

## Команды

| Команда | Что делает |
|---|---|
| `yarn solve` | открыть следующую нерешённую задачу и гонять только её тесты |
| `yarn solve ARR-14` | то же для конкретной задачи |
| `yarn solve ARR-14 -n` | без открытия редактора, только тесты |
| `yarn t` | watch по всем задачам сразу (шумно), `yarn t 02-arrays` — по одному паку |
| `yarn test` | однократный прогон всех тестов |
| `yarn task` | список паков |
| `yarn task ARR` | список задач пака |
| `yarn task ARR-14` | карточка задачи: условие + заготовка + тесты |
| `yarn task ARR-14 -c` | то же, но сразу в буфер обмена |
| `yarn task ARR-14 -s` | карточка вместе с эталонным решением |
| `yarn task next` | первая нерешённая задача |
| `yarn progress` | где ты сейчас + перезапись `PROGRESS.md` |
| `yarn progress ARR` | то же, но с разбивкой по задачам пака |
| `yarn verify` | самопроверка тренажёра: тесты и типы против эталонов |
| `yarn types` | проверка типов; красное в нерешённых задачах — норма |
| `yarn dev` | браузерная песочница для React-задач |

---

## Разбор задачи в бесплатном Клоде

```bash
yarn task ASY-07 -c
```

Карточка уже в буфере — вставляешь в claude.ai и отправляешь. Внутри готовый промпт:
Клод обязан сначала переформулировать условие, задать уточняющие вопросы и дать алгоритм словами,
и только потом код. Файлы прикладывать не нужно, карточка самодостаточная.

---

## Правила, без которых тренажёр не работает

1. **Сначала вслух.** Одним предложением: что делает функция и как. Не смог сказать — код писать рано.
2. **Не подглядывать в `solution.ts`,** пока не потратил норматив пака.
3. Посмотрел разбор — **сотри свой код и напиши заново сам.** Копирование не считается.
4. Задача сдана, когда написана **с нуля, без разбора, в норматив.** Первый проход не считается,
   считается повтор на следующий день.

---

## Протокол лайвкода на собесе

1. **Уточни условие.** Размер данных, можно ли мутировать вход, что делать с пустым и невалидным.
2. **Проговори пример.** «На вход `[1,2,3]`, жду `[2,4,6]`. Верно?»
3. **Алгоритм словами.** До кода.
4. **Пиши тупое рабочее решение.** Красоту и оптимизацию — потом и вслух.
5. **Проверь края:** пустой массив, один элемент, отрицательные, дубликаты, `null`.
6. **Назови сложность:** «один проход, O(n) по времени, O(n) по памяти из-за Set».

Убивает собес: молчание, попытка сразу писать идеально, «я не знаю» без продолжения.
Рабочая формулировка: «Точно не помню синтаксис, но идея такая: … Сейчас напишу как помню, потом поправлю.»

---

## Паки

| Код | Тема | Задач |
|---|---|---|
| `BAS` | база: числа, условия, циклы | 24 |
| `ARR` | массивы, иммутабельные правки | 32 |
| `STR` | строки и форматирование текста | 28 |
| `OBJ` | объекты, вложенность | 28 |
| `FUN` | замыкания, this, каррирование, композиция | 24 |
| `ASY` | промисы, async/await, отмена | 26 |
| `COL` | Map, Set, WeakMap, итераторы, генераторы | 18 |
| `REC` | рекурсия, деревья, обходы | 18 |
| `ALG` | два указателя, окно, бинпоиск, динамика | 26 |
| `UTL` | debounce, throttle, deepClone, EventEmitter, LRU | 24 |
| `FMT` | деньги, даты, склонения, длительности | 22 |
| `DAT` | фильтры, сортировки, пагинация, нормализация | 24 |
| `API` | запрос, отмена, ретраи, кэш, ошибки | 20 |
| `VAL` | валидация форм и данных | 16 |
| `BRW` | URL, storage, DOM, делегирование, фокус | 18 |
| `TS` | дженерики, utility types, сужение | 26 |
| `RB` | React: основы | 26 |
| `RH` | React: кастомные хуки | 26 |
| `RC` | React: компоненты с реальных собесов | 18 |
| `RP` | React: производительность | 16 |
| `RX` | React: контекст, составные компоненты, порталы | 18 |
| `QZ` | что выведет: event loop, this, приведение типов | 40 |

Порядок прохождения и расписание по дням — в [PLAN.md](./PLAN.md).
Рядом лежит отдельное приложение `next-lab` — база Next.js App Router на практике.

### Два пака работают иначе

**`TS` — типы.** Задачи 13-26 проверяет компилятор, а не рантайм. Строки
`type _TS13 = Expect<Equal<...>>` — это и есть тесты, удалять их нельзя. Пока тип неверен,
строка красная прямо в редакторе, а `yarn progress` покажет у задачи пометку `[типы: N]`.

**`QZ` — что выведет.** Здесь ничего не реализуют, здесь предсказывают. Тесты выполняют тот же код
и сравнивают твой ответ с реальным поведением движка — спорить не с кем.

---

## Файлы

| Файл | Зачем |
|---|---|
| [PLAN.md](./PLAN.md) | расписание: режим «собес через 3 дня» и режим «две недели» |
| [ROADMAP.md](./ROADMAP.md) | все паки и их статус |
| [PROGRESS.md](./PROGRESS.md) | текущий прогресс, генерируется `yarn progress`, руками не править |

---

## Стек

Vite 8 · React 19 · TypeScript 5.9 · Vitest 5 · happy-dom · Testing Library 16.

Vitest не случайно: это стандарт тестов во фронтенде, и умение написать тест — отдельный плюс на собесе.
