import { todo } from '../../../shared/kit'

// #region TSB-08 | Кортеж | ★☆☆
/**
 * Поменять местами элементы пары. Тип на входе и на выходе разный — это кортеж,
 * у него фиксированы и длина, и тип каждой позиции.
 *
 * Примеры:
 *   swap(['a', 1]) → [1, 'a']
 */
export const swap = (pair: [string, number]): [number, string] => todo()
// #endregion
