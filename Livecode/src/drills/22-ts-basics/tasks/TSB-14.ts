import { todo } from '../../../shared/kit'

// #region TSB-14 | Record и индексная сигнатура | ★★☆
/**
 * Посчитать, сколько раз встречается каждое слово.
 * Record<string, number> — объект с произвольными строковыми ключами и числовыми значениями.
 *
 * Примеры:
 *   countBy(['a', 'b', 'a']) → { a: 2, b: 1 }
 */
export const countBy = (words: string[]): Record<string, number> => todo()
// #endregion
