import { todo } from '../../../shared/kit'

// #region TSB-03 | Необязательный параметр | ★☆☆
/**
 * Поприветствовать. Если приветствие не передали — подставить «Привет».
 * Значение по умолчанию делает параметр необязательным само, знак ? писать не нужно.
 *
 * Примеры:
 *   greet('Аня')             → 'Привет, Аня!'
 *   greet('Аня', 'Здарова')  → 'Здарова, Аня!'
 */
export const greet = (name: string, greeting?: string): string => todo()
// #endregion
