/**
 * ПРАВИЛА ПАКА
 * 1. Задачи 01-20 проверяются тестами: `yarn t`.
 * 2. Задачи 21-30 — чистые типы, их проверяет компилятор: `yarn typecheck`.
 *    Строки вида `type _TSB21 = Expect<Equal<...>>` — это и есть тесты. НЕ УДАЛЯЙ их:
 *    пока тип написан неверно, строка краснеет прямо в редакторе.
 * 3. `any` запрещён во всём паке. Не знаешь тип — это `unknown` плюс сужение.
 * 4. `as` — крайняя мера. Сначала type guard, потом уже приведение.
 * 5. Типы полей не переписывать руками там, где есть готовая utility: Pick, Omit, Partial.
 */
