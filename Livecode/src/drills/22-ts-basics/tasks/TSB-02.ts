import { todo } from '../../../shared/kit'

// #region TSB-02 | Массив чисел | ★☆☆
/**
 * Сложить все числа массива. Пустой массив даёт 0.
 * number[] — массив, каждый элемент которого число.
 *
 * Примеры:
 *   sumAll([1, 2, 3]) → 6
 *   sumAll([])        → 0
 */
export const sumAll = (list: number[]): number => todo()
// #endregion
